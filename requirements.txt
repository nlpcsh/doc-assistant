# Requires Python 3.10 or later
# Core dependencies
pillow>=9.0.0
PyMuPDF>=1.27.0  # PDF processing library (provides fitz module)
PyPDF2>=3.0.0
reportlab>=3.6.0
cryptography>=38.0.0
python-docx>=1.1.0
docxtpl>=0.16.0

docx2pdf>=0.1.8

tkcalendar>=1.3.0
tkinterdnd2>=0.6.2

# For enhanced PDF signing (optional but recommended)
pyhanko>=0.17.0  # Provides full X.509 certificate-based PDF signing
pyhanko-certvalidator>=0.16.0  # Certificate validation support

# Type hints
typing-extensions>=4.0.0
